# Cowork adapter (milestone M5 — not yet implemented)

Planned shape, per docs/SPEC.md §4.2:

- Cowork plugin manifest + a skill that calls `stretchbreak pick` at the start of multi-step tasks and `done` when the tool burst completes.
- `panel.html`: single-file, non-modal panel with the looping animation, cue and countdown ring.
- `settings.html`: preferences UI that reads/writes `~/.stretchbreak/config.json` (shared with Claude Code).

Contributions welcome; open an issue to claim a piece.
